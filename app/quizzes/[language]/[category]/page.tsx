'use client'

// import { useParams } from 'next/navigation'
// import { useQuestionStore } from '@/app/store/questiionStore'
// import styles from '../../quizzes.module.css'

export default function Quizzes () {
  // const { language, category } = useParams()
  // const questions = useQuestionStore((state) => state.questions)
  // const setQuestions = useQuestionStore((state)=> state.setQuestions)
  // const setIsCorrect = useQuestionStore((state)=> state.setIsCorrect)
  // const isCorrect = useQuestionStore((state) => state.isCorrect)

  return (
    <div>
     {/* progress bar */}

     {/* question section */}

     {/* pop up integration */}
    </div>
  )
}
